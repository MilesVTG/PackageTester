import React from 'react';
import {View, Text} from 'react-native';
import styles from '../../styles/mainStyles';

const ItemScreen = props => {
  return (
    <View style={styles.screen}>
      <Text>The Item Screen</Text>
    </View>
  );
};

export default ItemScreen;
